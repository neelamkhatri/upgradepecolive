{#
/**
 * @file
 * Theme implementation for a 'webform' element.
 *
 * This is an copy of the webform.html.twig theme_wrapper which includes the
 * 'title_prefix' and 'title_suffix' variables needed for
 * contextual links to appear.
 *
 * Available variables
 * - attributes: A list of HTML attributes for the wrapper element.
 * - children: The child elements of the webform.
 * - title_prefix: Additional output populated by modules, intended to be
 *   displayed in front of the main title tag that appears in the template.
 * - title_suffix: Additional output populated by modules, intended to be
 *   displayed after the main title tag that appears in the template.
 *
 * @see template_preprocess_webform()
 * @see _webform_form_after_build()
 *
 * @ingroup themeable
 */
#}


<div class="main overflow-hidden m-0">
<div class="container-fluid py-5">
<div class="row">
<div class="container">
<div class="row justify-content-center">
<div class="col-md-7 mb-3 text-center common-text signUp">
<h1>Sign up for<br />
<span>updates!</span></h1>
</div>
<div class="col-md-7 mb-4 common-text signUp contactUsForm">
<p>Get news from Phillips Edison &amp; Company in your inbox.</p>
{{ element.elements.description }}
<form{{ attributes.addClass('mt-5') }}>
    {{ title_prefix }}
    {#{ children }#}
    {#{ element.elements|without('description') }#}

    {{ element.progress }}
    {{ element.form_token }}
    {{ element.form_build_id }}
    {{ element.form_id }}
    
    {{ element.elements.first_name }}
    {{ element.elements.last_name }}
    {{ element.elements.email }}

    
    {{ element.elements.company }}
     {{ element.elements.role }}
  
   <div class="d-inline-block w-100 text-center"> {{ element.elements.actions }}
    {{ title_suffix }}
    </div>
</form>


<p class="pl-0 mt-5"><small>By submitting this form, you are consenting to receive marketing emails from: Phillips Edison &amp; Co, 11501 Northlake Drive, Cincinnati, OH, 45249, US. You can revoke your consent to receive emails at any time by using the unsubscribe link, found at the bottom of every email.</small></p>
</div>
</div>
</div>
</div>
</div>
</div>